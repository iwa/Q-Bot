<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link href="https://fonts.googleapis.com/css?family=Nunito:300,400,600&display=swap" rel="stylesheet">
    <script src="https://kit.fontawesome.com/0de1f1c460.js" crossorigin="anonymous"></script>
    <style type="text/css">
    body { margin: 0; }
    .box { border: 4px solid #FFFFFF; border-radius: 3px; color: white; width: 500px; height: 350px; font-family: 'Nunito', sans-serif; background-color: #8BC6EC; background-image: linear-gradient(135deg, <%=firstColor%> 0%, <%=secondColor%> 100%); }
    #pfp { position: absolute; height: 140px; width: 140px; border-radius: 50%; }
    .box > section { position: absolute; top: 14px; left: 14px; width: 480px; height: 330px; background-color: rgba(0, 0, 0, 0.15); }
    header > div { position: absolute; top: 10px; left: 150px; display: table; width: 320px; height: 80px; }
    header > div > h4 { margin: 0; font-size: 28pt; font-weight: 600; display: table-cell; line-height: 1.1; text-align: left; vertical-align: middle; }
    header > div > h4 > i { padding: 0 0.4em 0 0; font-size: 18pt; vertical-align: middle; display: inline-block; height: 1.3em; color: rgba(255, 255, 255, 0.8); }
    header > section > h5 { position: absolute; margin: 0; top: 94px; left: 155px; font-size: 17pt; font-weight: 300; display: block; text-align: left; letter-spacing: 2px; }
    header > section > h5 > i { font-size: 14pt; }
    header > section > #xp { height: 8px; margin: 0; position: absolute; top: 120px; left: 150px; text-align: left; border: 1px solid rgba(255,255,255,0.4); border-radius: 0.2em; width: 320px; }
    header > section > #xp::-webkit-progress-value { background: rgba(255, 255, 255, 0.85); border-radius: 0.2em; }
    header > section > #xp::-webkit-progress-bar { background: rgba(156, 160, 162, 0.425); border-radius: 0.2em; box-shadow: 0 2px 5px rgba(0, 0, 0, 0.25) inset; }
    main > table { position: absolute; top: 170px; left: 10px; width: 460px; text-align: center; font-size: 16pt; }
    th { font-weight: 600; margin: 0 0 10px 0; }
    td { font-weight: 300; }
    footer > p { position: absolute; top: 315px; width: 480px; margin: 0; text-align: center; letter-spacing: 1px; color: rgba(255, 255, 255, 0.15); font-size: 10pt; }
    </style>
</head>
<body>

    <div class="box">
        <section>
            <header>
                <img src="<%= user.avatar %>" id="pfp" />
                <div>
                    <h4><%- user.icon %><%= user.username %></h4>
                </div>
                <section>
                    <h5>Level <i class="fas fa-infinity"></i></h5>
                    <progress id="xp" value="100" max="100"></progress>
                </section>
            </header>
            <main>
                <table>
                    <tr>
                        <th>Hugs :</th>
                        <th>Pats :</th>
                        <th>Boops :</th>
                    </tr>
                    <tr>
                        <td><%= user.hug %></td>
                        <td><%= user.pat %></td>
                        <td><%= user.boop %></td>
                    </tr>
                    <tr>
                        <th>Slaps :</th>
                        <th>?thanks :</th>
                    </tr>
                    <tr>
                        <td><%= user.slap %></td>
                        <td><%= thanksiwa %></td>
                    </tr>
                </table>
            </main>
            <footer>
                <p id="copyright">Made with <i class="far fa-heart"></i> by iwa</p>
            </footer>
        </section>
    </div>

</body>
</html>