image: node:latest

stages:
  - dependencies
  - build
  - lint

install:
  stage: dependencies
  script: npm install --only=dev

compile:
  stage: build
  script:
    - npm install -g typescript
    - tsc

eslint:
  stage: lint
  script:
    - npm install -g eslint
    - eslint build/**/*.js